package com.iryna.moviesapp.navigation

class NavigationTest {
/*
    @get:Rule
    val composeTestRule = createComposeRule()
    lateinit var navController: TestNavHostController

    @Before
    fun setupWorkersAppNavHost() {
        composeTestRule.setContent {
            navController = TestNavHostController(LocalContext.current)
            navController.navigatorProvider.addNavigator(ComposeNavigator())
            WorkersApp(navController = navController)
        }
    }

    // Unit test
    @Test
    fun workersApp_verifyStartDestination() {
        composeTestRule
            .onNodeWithTag(NavItem.SPLASH.route)
            .assertIsDisplayed()
    }

    // Additional navigation tests can be added here*/
}
